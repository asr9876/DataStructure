#include <iostream>
using namespace std;
bool Bsearch(int arr[], int size, int key ){
    for(int i = 0 ; i < size; i++){
        if(key == arr[i]){
            return true;
        }
    }return false;
}
int main() {
   //BINARY SEARCH 
   int key ;
   cin>>key ; 
   int arr[7] = {34,23,543,12,0,4,1};
   if(Bsearch(arr,7,key)){
       cout<<"present";
   }else{
       cout<<"not present";
   }
    return 0;
}
